spring:
  application:
    name: backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/kynghesat}
    username: ${SPRING_DATASOURCE_USERNAME:kynghesat}
    password: ${SPRING_DATASOURCE_PASSWORD:kynghesat}
    hikari:
      # Keep a small pool of connections warm so requests don't pay a fresh
      # TCP+TLS+auth handshake on every call — this matters a lot when the
      # database is a network hop away (e.g. a managed Postgres in another region).
      minimum-idle: 3
      maximum-pool-size: 10
      keepalive-time: 30000
      connection-timeout: 10000
      validation-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:change-this-dev-secret-key-please-override-in-prod-min-256-bits}
    expiration-minutes: 720
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  uploads:
    dir: ${UPLOADS_DIR:uploads}
    public-base-url: ${UPLOADS_PUBLIC_BASE_URL:http://localhost:8080/uploads}
